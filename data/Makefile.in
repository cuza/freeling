# Makefile.in generated by automake 1.11.3 from Makefile.am.
# @configure_input@

# Copyright (C) 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002,
# 2003, 2004, 2005, 2006, 2007, 2008, 2009, 2010, 2011 Free Software
# Foundation, Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@

VPATH = @srcdir@
pkgdatadir = $(datadir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkglibexecdir = $(libexecdir)/@PACKAGE@
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = @build@
host_triplet = @host@
subdir = data
DIST_COMMON = $(srcdir)/Makefile.am $(srcdir)/Makefile.in
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
mkinstalldirs = $(install_sh) -d
CONFIG_CLEAN_FILES =
CONFIG_CLEAN_VPATH_FILES =
SOURCES =
DIST_SOURCES =
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = f=`echo $$p | sed -e 's|^.*/||'`;
am__install_max = 40
am__nobase_strip_setup = \
  srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*|]/\\\\&/g'`
am__nobase_strip = \
  for p in $$list; do echo "$$p"; done | sed -e "s|$$srcdirstrip/||"
am__nobase_list = $(am__nobase_strip_setup); \
  for p in $$list; do echo "$$p $$p"; done | \
  sed "s| $$srcdirstrip/| |;"' / .*\//!s/ .*/ ./; s,\( .*\)/[^/]*$$,\1,' | \
  $(AWK) 'BEGIN { files["."] = "" } { files[$$2] = files[$$2] " " $$1; \
    if (++n[$$2] == $(am__install_max)) \
      { print $$2, files[$$2]; n[$$2] = 0; files[$$2] = "" } } \
    END { for (dir in files) print dir, files[dir] }'
am__base_list = \
  sed '$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;s/\n/ /g' | \
  sed '$$!N;$$!N;$$!N;$$!N;s/\n/ /g'
am__uninstall_files_from_dir = { \
  test -z "$$files" \
    || { test ! -d "$$dir" && test ! -f "$$dir" && test ! -r "$$dir"; } \
    || { echo " ( cd '$$dir' && rm -f" $$files ")"; \
         $(am__cd) "$$dir" && rm -f $$files; }; \
  }
am__installdirs = "$(DESTDIR)$(aschunkdir)" "$(DESTDIR)$(asdatadir)" \
	"$(DESTDIR)$(asdepdatadir)" "$(DESTDIR)$(asnecdatadir)" \
	"$(DESTDIR)$(cachunkdir)" "$(DESTDIR)$(cadatadir)" \
	"$(DESTDIR)$(cadepdatadir)" "$(DESTDIR)$(cadnercdatadir)" \
	"$(DESTDIR)$(canecdatadir)" "$(DESTDIR)$(canerdatadir)" \
	"$(DESTDIR)$(commonalternativesdatadir)" \
	"$(DESTDIR)$(commondatadir)" \
	"$(DESTDIR)$(commonlangidentdatadir)" \
	"$(DESTDIR)$(commonnecdatadir)" "$(DESTDIR)$(configdatadir)" \
	"$(DESTDIR)$(csdatadir)" "$(DESTDIR)$(cydatadir)" \
	"$(DESTDIR)$(cynecdatadir)" "$(DESTDIR)$(enchunkdir)" \
	"$(DESTDIR)$(endatadir)" "$(DESTDIR)$(endepdatadir)" \
	"$(DESTDIR)$(endnercdatadir)" "$(DESTDIR)$(ennecdatadir)" \
	"$(DESTDIR)$(ennerdatadir)" "$(DESTDIR)$(eschunkdir)" \
	"$(DESTDIR)$(escorefdatadir)" "$(DESTDIR)$(esdatadir)" \
	"$(DESTDIR)$(esdepdatadir)" "$(DESTDIR)$(esdnercdatadir)" \
	"$(DESTDIR)$(esnecdatadir)" "$(DESTDIR)$(esnerdatadir)" \
	"$(DESTDIR)$(frdatadir)" "$(DESTDIR)$(glchunkdir)" \
	"$(DESTDIR)$(gldatadir)" "$(DESTDIR)$(gldepdatadir)" \
	"$(DESTDIR)$(gldnercdatadir)" "$(DESTDIR)$(glnecdatadir)" \
	"$(DESTDIR)$(glnerdatadir)" "$(DESTDIR)$(itdatadir)" \
	"$(DESTDIR)$(itnecdatadir)" "$(DESTDIR)$(oldesdatadir)" \
	"$(DESTDIR)$(pkgdatadir)" "$(DESTDIR)$(ptchunkdir)" \
	"$(DESTDIR)$(ptdatadir)" "$(DESTDIR)$(ptdnercdatadir)" \
	"$(DESTDIR)$(ptnecdatadir)" "$(DESTDIR)$(ptnerdatadir)" \
	"$(DESTDIR)$(rudatadir)"
DATA = $(aschunk_DATA) $(asdata_DATA) $(asdepdata_DATA) \
	$(asnecdata_DATA) $(cachunk_DATA) $(cadata_DATA) \
	$(cadepdata_DATA) $(cadnercdata_DATA) $(canecdata_DATA) \
	$(canerdata_DATA) $(commonalternativesdata_DATA) \
	$(commondata_DATA) $(commonlangidentdata_DATA) \
	$(commonnecdata_DATA) $(configdata_DATA) $(csdata_DATA) \
	$(cydata_DATA) $(cynecdata_DATA) $(enchunk_DATA) \
	$(endata_DATA) $(endepdata_DATA) $(endnercdata_DATA) \
	$(ennecdata_DATA) $(ennerdata_DATA) $(eschunk_DATA) \
	$(escorefdata_DATA) $(esdata_DATA) $(esdepdata_DATA) \
	$(esdnercdata_DATA) $(esnecdata_DATA) $(esnerdata_DATA) \
	$(frdata_DATA) $(glchunk_DATA) $(gldata_DATA) \
	$(gldepdata_DATA) $(gldnercdata_DATA) $(glnecdata_DATA) \
	$(glnerdata_DATA) $(itdata_DATA) $(itnecdata_DATA) \
	$(oldesdata_DATA) $(pkgdata_DATA) $(ptchunk_DATA) \
	$(ptdata_DATA) $(ptdnercdata_DATA) $(ptnecdata_DATA) \
	$(ptnerdata_DATA) $(rudata_DATA)
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
ACLOCAL = @ACLOCAL@
AMTAR = @AMTAR@
AR = @AR@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
CC = @CC@
CCDEPMODE = @CCDEPMODE@
CFLAGS = @CFLAGS@
CPP = @CPP@
CPPFLAGS = @CPPFLAGS@
CXX = @CXX@
CXXCPP = @CXXCPP@
CXXDEPMODE = @CXXDEPMODE@
CXXFLAGS = @CXXFLAGS@
CYGPATH_W = @CYGPATH_W@
DEFS = @DEFS@
DEPDIR = @DEPDIR@
DLLTOOL = @DLLTOOL@
DSYMUTIL = @DSYMUTIL@
DUMPBIN = @DUMPBIN@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
EGREP = @EGREP@
EXEEXT = @EXEEXT@
FGREP = @FGREP@
GREP = @GREP@
INSTALL = @INSTALL@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_STRIP_PROGRAM = @INSTALL_STRIP_PROGRAM@
LD = @LD@
LDFLAGS = @LDFLAGS@
LIBOBJS = @LIBOBJS@
LIBS = @LIBS@
LIBTOOL = @LIBTOOL@
LIPO = @LIPO@
LN_S = @LN_S@
LTLIBOBJS = @LTLIBOBJS@
MAKEINFO = @MAKEINFO@
MANIFEST_TOOL = @MANIFEST_TOOL@
MKDIR_P = @MKDIR_P@
NM = @NM@
NMEDIT = @NMEDIT@
OBJDUMP = @OBJDUMP@
OBJEXT = @OBJEXT@
OTOOL = @OTOOL@
OTOOL64 = @OTOOL64@
PACKAGE = @PACKAGE@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_URL = @PACKAGE_URL@
PACKAGE_VERSION = @PACKAGE_VERSION@
PATH_SEPARATOR = @PATH_SEPARATOR@
RANLIB = @RANLIB@
SED = @SED@
SET_MAKE = @SET_MAKE@
SHELL = @SHELL@
STRIP = @STRIP@
VERSION = @VERSION@
abs_builddir = @abs_builddir@
abs_srcdir = @abs_srcdir@
abs_top_builddir = @abs_top_builddir@
abs_top_srcdir = @abs_top_srcdir@
ac_ct_AR = @ac_ct_AR@
ac_ct_CC = @ac_ct_CC@
ac_ct_CXX = @ac_ct_CXX@
ac_ct_DUMPBIN = @ac_ct_DUMPBIN@
am__include = @am__include@
am__leading_dot = @am__leading_dot@
am__quote = @am__quote@
am__tar = @am__tar@
am__untar = @am__untar@
bindir = @bindir@
build = @build@
build_alias = @build_alias@
build_cpu = @build_cpu@
build_os = @build_os@
build_vendor = @build_vendor@
builddir = @builddir@
datadir = @datadir@
datarootdir = @datarootdir@
docdir = @docdir@
dvidir = @dvidir@
exec_prefix = @exec_prefix@
host = @host@
host_alias = @host_alias@
host_cpu = @host_cpu@
host_os = @host_os@
host_vendor = @host_vendor@
htmldir = @htmldir@
includedir = @includedir@
infodir = @infodir@
install_sh = @install_sh@
libdir = @libdir@
libexecdir = @libexecdir@
localedir = @localedir@
localstatedir = @localstatedir@
mandir = @mandir@
mkdir_p = @mkdir_p@
oldincludedir = @oldincludedir@
pdfdir = @pdfdir@
prefix = @prefix@
program_transform_name = @program_transform_name@
psdir = @psdir@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
srcdir = @srcdir@
sysconfdir = @sysconfdir@
target_alias = @target_alias@
top_build_prefix = @top_build_prefix@
top_builddir = @top_builddir@
top_srcdir = @top_srcdir@
languages = as ca cs cy en es fr gl it pt ru
variants = es/old-es
pkgdata_DATA = 
asdatadir = $(pkgdatadir)/as
asdata_DATA = as/*.*
asnecdatadir = $(pkgdatadir)/as/nec
asnecdata_DATA = as/nec/*
aschunkdir = $(pkgdatadir)/as/chunker
aschunk_DATA = as/chunker/*
asdepdatadir = $(pkgdatadir)/as/dep
asdepdata_DATA = as/dep/*
cadatadir = $(pkgdatadir)/ca
cadata_DATA = ca/*.*
canercdatadir = $(pkgdatadir)/ca/nerc
canecdatadir = $(pkgdatadir)/ca/nerc/nec
canerdatadir = $(pkgdatadir)/ca/nerc/ner
cadnercdatadir = $(pkgdatadir)/ca/nerc/data
canecdata_DATA = ca/nerc/nec/*
canerdata_DATA = ca/nerc/ner/*
cadnercdata_DATA = ca/nerc/data/*
cachunkdir = $(pkgdatadir)/ca/chunker
cachunk_DATA = ca/chunker/*
cadepdatadir = $(pkgdatadir)/ca/dep
cadepdata_DATA = ca/dep/*
csdatadir = $(pkgdatadir)/cs
csdata_DATA = cs/*.*
cydatadir = $(pkgdatadir)/cy
cydata_DATA = cy/*.*
cynecdatadir = $(pkgdatadir)/cy/nec
cynecdata_DATA = cy/nec/*
endatadir = $(pkgdatadir)/en
endata_DATA = en/*.*
ennercdatadir = $(pkgdatadir)/en/nerc
ennecdatadir = $(pkgdatadir)/en/nerc/nec
ennerdatadir = $(pkgdatadir)/en/nerc/ner
endnercdatadir = $(pkgdatadir)/en/nerc/data
ennecdata_DATA = en/nerc/nec/*
ennerdata_DATA = en/nerc/ner/*
endnercdata_DATA = en/nerc/data/*
enchunkdir = $(pkgdatadir)/en/chunker
enchunk_DATA = en/chunker/*
endepdatadir = $(pkgdatadir)/en/dep
endepdata_DATA = en/dep/*
esdatadir = $(pkgdatadir)/es
esdata_DATA = es/*.*
esnercdatadir = $(pkgdatadir)/es/nerc
esnecdatadir = $(pkgdatadir)/es/nerc/nec
esnerdatadir = $(pkgdatadir)/es/nerc/ner
esdnercdatadir = $(pkgdatadir)/es/nerc/data
esnecdata_DATA = es/nerc/nec/*
esnerdata_DATA = es/nerc/ner/*
esdnercdata_DATA = es/nerc/data/*
eschunkdir = $(pkgdatadir)/es/chunker
eschunk_DATA = es/chunker/*
esdepdatadir = $(pkgdatadir)/es/dep
esdepdata_DATA = es/dep/*
escorefdatadir = $(pkgdatadir)/es/coref
escorefdata_DATA = es/coref/*
oldesdatadir = $(pkgdatadir)/es/old-es
oldesdata_DATA = es/old-es/*.*
frdatadir = $(pkgdatadir)/fr
frdata_DATA = fr/*.*
gldatadir = $(pkgdatadir)/gl
gldata_DATA = gl/*.*
glnercdatadir = $(pkgdatadir)/gl/nerc
glnecdatadir = $(pkgdatadir)/gl/nerc/nec
glnerdatadir = $(pkgdatadir)/gl/nerc/ner
gldnercdatadir = $(pkgdatadir)/gl/nerc/data
glnecdata_DATA = gl/nerc/nec/*
glnerdata_DATA = gl/nerc/ner/*
gldnercdata_DATA = gl/nerc/data/*
glchunkdir = $(pkgdatadir)/gl/chunker
glchunk_DATA = gl/chunker/*
gldepdatadir = $(pkgdatadir)/gl/dep
gldepdata_DATA = gl/dep/*
itdatadir = $(pkgdatadir)/it
itdata_DATA = it/*.*
itnecdatadir = $(pkgdatadir)/it/nec
itnecdata_DATA = it/nec/*
ptdatadir = $(pkgdatadir)/pt
ptdata_DATA = pt/*.*
ptnercdatadir = $(pkgdatadir)/pt/nerc
ptnecdatadir = $(pkgdatadir)/pt/nerc/nec
ptnerdatadir = $(pkgdatadir)/pt/nerc/ner
ptdnercdatadir = $(pkgdatadir)/pt/nerc/data
ptnecdata_DATA = pt/nerc/nec/*
ptnerdata_DATA = pt/nerc/ner/*
ptdnercdata_DATA = pt/nerc/data/*
ptchunkdir = $(pkgdatadir)/pt/chunker
ptchunk_DATA = pt/chunker/*
rudatadir = $(pkgdatadir)/ru
rudata_DATA = ru/*.*
commondatadir = $(pkgdatadir)/common
commondata_DATA = common/*.*
commonnecdatadir = $(pkgdatadir)/common/nec
commonnecdata_DATA = common/nec/*
commonalternativesdatadir = $(pkgdatadir)/common/alternatives
commonalternativesdata_DATA = common/alternatives/*
commonlangidentdatadir = $(pkgdatadir)/common/lang_ident
commonlangidentdata_DATA = common/lang_ident/*
configdatadir = $(pkgdatadir)/config
configdata_DATA = config/*
all: all-am

.SUFFIXES:
$(srcdir)/Makefile.in:  $(srcdir)/Makefile.am  $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      ( cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh ) \
	        && { if test -f $@; then exit 0; else break; fi; }; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --gnu data/Makefile'; \
	$(am__cd) $(top_srcdir) && \
	  $(AUTOMAKE) --gnu data/Makefile
.PRECIOUS: Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe);; \
	esac;

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure:  $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4):  $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(am__aclocal_m4_deps):

mostlyclean-libtool:
	-rm -f *.lo

clean-libtool:
	-rm -rf .libs _libs
install-aschunkDATA: $(aschunk_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(aschunkdir)" || $(MKDIR_P) "$(DESTDIR)$(aschunkdir)"
	@list='$(aschunk_DATA)'; test -n "$(aschunkdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(aschunkdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(aschunkdir)" || exit $$?; \
	done

uninstall-aschunkDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(aschunk_DATA)'; test -n "$(aschunkdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(aschunkdir)'; $(am__uninstall_files_from_dir)
install-asdataDATA: $(asdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(asdatadir)" || $(MKDIR_P) "$(DESTDIR)$(asdatadir)"
	@list='$(asdata_DATA)'; test -n "$(asdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(asdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(asdatadir)" || exit $$?; \
	done

uninstall-asdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(asdata_DATA)'; test -n "$(asdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(asdatadir)'; $(am__uninstall_files_from_dir)
install-asdepdataDATA: $(asdepdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(asdepdatadir)" || $(MKDIR_P) "$(DESTDIR)$(asdepdatadir)"
	@list='$(asdepdata_DATA)'; test -n "$(asdepdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(asdepdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(asdepdatadir)" || exit $$?; \
	done

uninstall-asdepdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(asdepdata_DATA)'; test -n "$(asdepdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(asdepdatadir)'; $(am__uninstall_files_from_dir)
install-asnecdataDATA: $(asnecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(asnecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(asnecdatadir)"
	@list='$(asnecdata_DATA)'; test -n "$(asnecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(asnecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(asnecdatadir)" || exit $$?; \
	done

uninstall-asnecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(asnecdata_DATA)'; test -n "$(asnecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(asnecdatadir)'; $(am__uninstall_files_from_dir)
install-cachunkDATA: $(cachunk_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(cachunkdir)" || $(MKDIR_P) "$(DESTDIR)$(cachunkdir)"
	@list='$(cachunk_DATA)'; test -n "$(cachunkdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cachunkdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cachunkdir)" || exit $$?; \
	done

uninstall-cachunkDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(cachunk_DATA)'; test -n "$(cachunkdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(cachunkdir)'; $(am__uninstall_files_from_dir)
install-cadataDATA: $(cadata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(cadatadir)" || $(MKDIR_P) "$(DESTDIR)$(cadatadir)"
	@list='$(cadata_DATA)'; test -n "$(cadatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cadatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cadatadir)" || exit $$?; \
	done

uninstall-cadataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(cadata_DATA)'; test -n "$(cadatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(cadatadir)'; $(am__uninstall_files_from_dir)
install-cadepdataDATA: $(cadepdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(cadepdatadir)" || $(MKDIR_P) "$(DESTDIR)$(cadepdatadir)"
	@list='$(cadepdata_DATA)'; test -n "$(cadepdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cadepdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cadepdatadir)" || exit $$?; \
	done

uninstall-cadepdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(cadepdata_DATA)'; test -n "$(cadepdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(cadepdatadir)'; $(am__uninstall_files_from_dir)
install-cadnercdataDATA: $(cadnercdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(cadnercdatadir)" || $(MKDIR_P) "$(DESTDIR)$(cadnercdatadir)"
	@list='$(cadnercdata_DATA)'; test -n "$(cadnercdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cadnercdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cadnercdatadir)" || exit $$?; \
	done

uninstall-cadnercdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(cadnercdata_DATA)'; test -n "$(cadnercdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(cadnercdatadir)'; $(am__uninstall_files_from_dir)
install-canecdataDATA: $(canecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(canecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(canecdatadir)"
	@list='$(canecdata_DATA)'; test -n "$(canecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(canecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(canecdatadir)" || exit $$?; \
	done

uninstall-canecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(canecdata_DATA)'; test -n "$(canecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(canecdatadir)'; $(am__uninstall_files_from_dir)
install-canerdataDATA: $(canerdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(canerdatadir)" || $(MKDIR_P) "$(DESTDIR)$(canerdatadir)"
	@list='$(canerdata_DATA)'; test -n "$(canerdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(canerdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(canerdatadir)" || exit $$?; \
	done

uninstall-canerdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(canerdata_DATA)'; test -n "$(canerdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(canerdatadir)'; $(am__uninstall_files_from_dir)
install-commonalternativesdataDATA: $(commonalternativesdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(commonalternativesdatadir)" || $(MKDIR_P) "$(DESTDIR)$(commonalternativesdatadir)"
	@list='$(commonalternativesdata_DATA)'; test -n "$(commonalternativesdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(commonalternativesdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(commonalternativesdatadir)" || exit $$?; \
	done

uninstall-commonalternativesdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(commonalternativesdata_DATA)'; test -n "$(commonalternativesdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(commonalternativesdatadir)'; $(am__uninstall_files_from_dir)
install-commondataDATA: $(commondata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(commondatadir)" || $(MKDIR_P) "$(DESTDIR)$(commondatadir)"
	@list='$(commondata_DATA)'; test -n "$(commondatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(commondatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(commondatadir)" || exit $$?; \
	done

uninstall-commondataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(commondata_DATA)'; test -n "$(commondatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(commondatadir)'; $(am__uninstall_files_from_dir)
install-commonlangidentdataDATA: $(commonlangidentdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(commonlangidentdatadir)" || $(MKDIR_P) "$(DESTDIR)$(commonlangidentdatadir)"
	@list='$(commonlangidentdata_DATA)'; test -n "$(commonlangidentdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(commonlangidentdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(commonlangidentdatadir)" || exit $$?; \
	done

uninstall-commonlangidentdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(commonlangidentdata_DATA)'; test -n "$(commonlangidentdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(commonlangidentdatadir)'; $(am__uninstall_files_from_dir)
install-commonnecdataDATA: $(commonnecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(commonnecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(commonnecdatadir)"
	@list='$(commonnecdata_DATA)'; test -n "$(commonnecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(commonnecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(commonnecdatadir)" || exit $$?; \
	done

uninstall-commonnecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(commonnecdata_DATA)'; test -n "$(commonnecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(commonnecdatadir)'; $(am__uninstall_files_from_dir)
install-configdataDATA: $(configdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(configdatadir)" || $(MKDIR_P) "$(DESTDIR)$(configdatadir)"
	@list='$(configdata_DATA)'; test -n "$(configdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(configdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(configdatadir)" || exit $$?; \
	done

uninstall-configdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(configdata_DATA)'; test -n "$(configdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(configdatadir)'; $(am__uninstall_files_from_dir)
install-csdataDATA: $(csdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(csdatadir)" || $(MKDIR_P) "$(DESTDIR)$(csdatadir)"
	@list='$(csdata_DATA)'; test -n "$(csdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(csdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(csdatadir)" || exit $$?; \
	done

uninstall-csdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(csdata_DATA)'; test -n "$(csdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(csdatadir)'; $(am__uninstall_files_from_dir)
install-cydataDATA: $(cydata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(cydatadir)" || $(MKDIR_P) "$(DESTDIR)$(cydatadir)"
	@list='$(cydata_DATA)'; test -n "$(cydatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cydatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cydatadir)" || exit $$?; \
	done

uninstall-cydataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(cydata_DATA)'; test -n "$(cydatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(cydatadir)'; $(am__uninstall_files_from_dir)
install-cynecdataDATA: $(cynecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(cynecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(cynecdatadir)"
	@list='$(cynecdata_DATA)'; test -n "$(cynecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(cynecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(cynecdatadir)" || exit $$?; \
	done

uninstall-cynecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(cynecdata_DATA)'; test -n "$(cynecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(cynecdatadir)'; $(am__uninstall_files_from_dir)
install-enchunkDATA: $(enchunk_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(enchunkdir)" || $(MKDIR_P) "$(DESTDIR)$(enchunkdir)"
	@list='$(enchunk_DATA)'; test -n "$(enchunkdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(enchunkdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(enchunkdir)" || exit $$?; \
	done

uninstall-enchunkDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(enchunk_DATA)'; test -n "$(enchunkdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(enchunkdir)'; $(am__uninstall_files_from_dir)
install-endataDATA: $(endata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(endatadir)" || $(MKDIR_P) "$(DESTDIR)$(endatadir)"
	@list='$(endata_DATA)'; test -n "$(endatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(endatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(endatadir)" || exit $$?; \
	done

uninstall-endataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(endata_DATA)'; test -n "$(endatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(endatadir)'; $(am__uninstall_files_from_dir)
install-endepdataDATA: $(endepdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(endepdatadir)" || $(MKDIR_P) "$(DESTDIR)$(endepdatadir)"
	@list='$(endepdata_DATA)'; test -n "$(endepdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(endepdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(endepdatadir)" || exit $$?; \
	done

uninstall-endepdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(endepdata_DATA)'; test -n "$(endepdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(endepdatadir)'; $(am__uninstall_files_from_dir)
install-endnercdataDATA: $(endnercdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(endnercdatadir)" || $(MKDIR_P) "$(DESTDIR)$(endnercdatadir)"
	@list='$(endnercdata_DATA)'; test -n "$(endnercdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(endnercdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(endnercdatadir)" || exit $$?; \
	done

uninstall-endnercdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(endnercdata_DATA)'; test -n "$(endnercdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(endnercdatadir)'; $(am__uninstall_files_from_dir)
install-ennecdataDATA: $(ennecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(ennecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(ennecdatadir)"
	@list='$(ennecdata_DATA)'; test -n "$(ennecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ennecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ennecdatadir)" || exit $$?; \
	done

uninstall-ennecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(ennecdata_DATA)'; test -n "$(ennecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(ennecdatadir)'; $(am__uninstall_files_from_dir)
install-ennerdataDATA: $(ennerdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(ennerdatadir)" || $(MKDIR_P) "$(DESTDIR)$(ennerdatadir)"
	@list='$(ennerdata_DATA)'; test -n "$(ennerdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ennerdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ennerdatadir)" || exit $$?; \
	done

uninstall-ennerdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(ennerdata_DATA)'; test -n "$(ennerdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(ennerdatadir)'; $(am__uninstall_files_from_dir)
install-eschunkDATA: $(eschunk_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(eschunkdir)" || $(MKDIR_P) "$(DESTDIR)$(eschunkdir)"
	@list='$(eschunk_DATA)'; test -n "$(eschunkdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(eschunkdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(eschunkdir)" || exit $$?; \
	done

uninstall-eschunkDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(eschunk_DATA)'; test -n "$(eschunkdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(eschunkdir)'; $(am__uninstall_files_from_dir)
install-escorefdataDATA: $(escorefdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(escorefdatadir)" || $(MKDIR_P) "$(DESTDIR)$(escorefdatadir)"
	@list='$(escorefdata_DATA)'; test -n "$(escorefdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(escorefdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(escorefdatadir)" || exit $$?; \
	done

uninstall-escorefdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(escorefdata_DATA)'; test -n "$(escorefdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(escorefdatadir)'; $(am__uninstall_files_from_dir)
install-esdataDATA: $(esdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(esdatadir)" || $(MKDIR_P) "$(DESTDIR)$(esdatadir)"
	@list='$(esdata_DATA)'; test -n "$(esdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(esdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(esdatadir)" || exit $$?; \
	done

uninstall-esdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(esdata_DATA)'; test -n "$(esdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(esdatadir)'; $(am__uninstall_files_from_dir)
install-esdepdataDATA: $(esdepdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(esdepdatadir)" || $(MKDIR_P) "$(DESTDIR)$(esdepdatadir)"
	@list='$(esdepdata_DATA)'; test -n "$(esdepdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(esdepdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(esdepdatadir)" || exit $$?; \
	done

uninstall-esdepdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(esdepdata_DATA)'; test -n "$(esdepdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(esdepdatadir)'; $(am__uninstall_files_from_dir)
install-esdnercdataDATA: $(esdnercdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(esdnercdatadir)" || $(MKDIR_P) "$(DESTDIR)$(esdnercdatadir)"
	@list='$(esdnercdata_DATA)'; test -n "$(esdnercdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(esdnercdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(esdnercdatadir)" || exit $$?; \
	done

uninstall-esdnercdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(esdnercdata_DATA)'; test -n "$(esdnercdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(esdnercdatadir)'; $(am__uninstall_files_from_dir)
install-esnecdataDATA: $(esnecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(esnecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(esnecdatadir)"
	@list='$(esnecdata_DATA)'; test -n "$(esnecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(esnecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(esnecdatadir)" || exit $$?; \
	done

uninstall-esnecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(esnecdata_DATA)'; test -n "$(esnecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(esnecdatadir)'; $(am__uninstall_files_from_dir)
install-esnerdataDATA: $(esnerdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(esnerdatadir)" || $(MKDIR_P) "$(DESTDIR)$(esnerdatadir)"
	@list='$(esnerdata_DATA)'; test -n "$(esnerdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(esnerdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(esnerdatadir)" || exit $$?; \
	done

uninstall-esnerdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(esnerdata_DATA)'; test -n "$(esnerdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(esnerdatadir)'; $(am__uninstall_files_from_dir)
install-frdataDATA: $(frdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(frdatadir)" || $(MKDIR_P) "$(DESTDIR)$(frdatadir)"
	@list='$(frdata_DATA)'; test -n "$(frdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(frdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(frdatadir)" || exit $$?; \
	done

uninstall-frdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(frdata_DATA)'; test -n "$(frdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(frdatadir)'; $(am__uninstall_files_from_dir)
install-glchunkDATA: $(glchunk_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(glchunkdir)" || $(MKDIR_P) "$(DESTDIR)$(glchunkdir)"
	@list='$(glchunk_DATA)'; test -n "$(glchunkdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(glchunkdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(glchunkdir)" || exit $$?; \
	done

uninstall-glchunkDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(glchunk_DATA)'; test -n "$(glchunkdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(glchunkdir)'; $(am__uninstall_files_from_dir)
install-gldataDATA: $(gldata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(gldatadir)" || $(MKDIR_P) "$(DESTDIR)$(gldatadir)"
	@list='$(gldata_DATA)'; test -n "$(gldatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gldatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gldatadir)" || exit $$?; \
	done

uninstall-gldataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(gldata_DATA)'; test -n "$(gldatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(gldatadir)'; $(am__uninstall_files_from_dir)
install-gldepdataDATA: $(gldepdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(gldepdatadir)" || $(MKDIR_P) "$(DESTDIR)$(gldepdatadir)"
	@list='$(gldepdata_DATA)'; test -n "$(gldepdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gldepdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gldepdatadir)" || exit $$?; \
	done

uninstall-gldepdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(gldepdata_DATA)'; test -n "$(gldepdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(gldepdatadir)'; $(am__uninstall_files_from_dir)
install-gldnercdataDATA: $(gldnercdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(gldnercdatadir)" || $(MKDIR_P) "$(DESTDIR)$(gldnercdatadir)"
	@list='$(gldnercdata_DATA)'; test -n "$(gldnercdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(gldnercdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(gldnercdatadir)" || exit $$?; \
	done

uninstall-gldnercdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(gldnercdata_DATA)'; test -n "$(gldnercdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(gldnercdatadir)'; $(am__uninstall_files_from_dir)
install-glnecdataDATA: $(glnecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(glnecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(glnecdatadir)"
	@list='$(glnecdata_DATA)'; test -n "$(glnecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(glnecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(glnecdatadir)" || exit $$?; \
	done

uninstall-glnecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(glnecdata_DATA)'; test -n "$(glnecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(glnecdatadir)'; $(am__uninstall_files_from_dir)
install-glnerdataDATA: $(glnerdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(glnerdatadir)" || $(MKDIR_P) "$(DESTDIR)$(glnerdatadir)"
	@list='$(glnerdata_DATA)'; test -n "$(glnerdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(glnerdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(glnerdatadir)" || exit $$?; \
	done

uninstall-glnerdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(glnerdata_DATA)'; test -n "$(glnerdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(glnerdatadir)'; $(am__uninstall_files_from_dir)
install-itdataDATA: $(itdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(itdatadir)" || $(MKDIR_P) "$(DESTDIR)$(itdatadir)"
	@list='$(itdata_DATA)'; test -n "$(itdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(itdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(itdatadir)" || exit $$?; \
	done

uninstall-itdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(itdata_DATA)'; test -n "$(itdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(itdatadir)'; $(am__uninstall_files_from_dir)
install-itnecdataDATA: $(itnecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(itnecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(itnecdatadir)"
	@list='$(itnecdata_DATA)'; test -n "$(itnecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(itnecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(itnecdatadir)" || exit $$?; \
	done

uninstall-itnecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(itnecdata_DATA)'; test -n "$(itnecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(itnecdatadir)'; $(am__uninstall_files_from_dir)
install-oldesdataDATA: $(oldesdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(oldesdatadir)" || $(MKDIR_P) "$(DESTDIR)$(oldesdatadir)"
	@list='$(oldesdata_DATA)'; test -n "$(oldesdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(oldesdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(oldesdatadir)" || exit $$?; \
	done

uninstall-oldesdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(oldesdata_DATA)'; test -n "$(oldesdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(oldesdatadir)'; $(am__uninstall_files_from_dir)
install-pkgdataDATA: $(pkgdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(pkgdatadir)" || $(MKDIR_P) "$(DESTDIR)$(pkgdatadir)"
	@list='$(pkgdata_DATA)'; test -n "$(pkgdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(pkgdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(pkgdatadir)" || exit $$?; \
	done

uninstall-pkgdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(pkgdata_DATA)'; test -n "$(pkgdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(pkgdatadir)'; $(am__uninstall_files_from_dir)
install-ptchunkDATA: $(ptchunk_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(ptchunkdir)" || $(MKDIR_P) "$(DESTDIR)$(ptchunkdir)"
	@list='$(ptchunk_DATA)'; test -n "$(ptchunkdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ptchunkdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ptchunkdir)" || exit $$?; \
	done

uninstall-ptchunkDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(ptchunk_DATA)'; test -n "$(ptchunkdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(ptchunkdir)'; $(am__uninstall_files_from_dir)
install-ptdataDATA: $(ptdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(ptdatadir)" || $(MKDIR_P) "$(DESTDIR)$(ptdatadir)"
	@list='$(ptdata_DATA)'; test -n "$(ptdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ptdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ptdatadir)" || exit $$?; \
	done

uninstall-ptdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(ptdata_DATA)'; test -n "$(ptdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(ptdatadir)'; $(am__uninstall_files_from_dir)
install-ptdnercdataDATA: $(ptdnercdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(ptdnercdatadir)" || $(MKDIR_P) "$(DESTDIR)$(ptdnercdatadir)"
	@list='$(ptdnercdata_DATA)'; test -n "$(ptdnercdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ptdnercdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ptdnercdatadir)" || exit $$?; \
	done

uninstall-ptdnercdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(ptdnercdata_DATA)'; test -n "$(ptdnercdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(ptdnercdatadir)'; $(am__uninstall_files_from_dir)
install-ptnecdataDATA: $(ptnecdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(ptnecdatadir)" || $(MKDIR_P) "$(DESTDIR)$(ptnecdatadir)"
	@list='$(ptnecdata_DATA)'; test -n "$(ptnecdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ptnecdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ptnecdatadir)" || exit $$?; \
	done

uninstall-ptnecdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(ptnecdata_DATA)'; test -n "$(ptnecdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(ptnecdatadir)'; $(am__uninstall_files_from_dir)
install-ptnerdataDATA: $(ptnerdata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(ptnerdatadir)" || $(MKDIR_P) "$(DESTDIR)$(ptnerdatadir)"
	@list='$(ptnerdata_DATA)'; test -n "$(ptnerdatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(ptnerdatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(ptnerdatadir)" || exit $$?; \
	done

uninstall-ptnerdataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(ptnerdata_DATA)'; test -n "$(ptnerdatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(ptnerdatadir)'; $(am__uninstall_files_from_dir)
install-rudataDATA: $(rudata_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(rudatadir)" || $(MKDIR_P) "$(DESTDIR)$(rudatadir)"
	@list='$(rudata_DATA)'; test -n "$(rudatadir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(rudatadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(rudatadir)" || exit $$?; \
	done

uninstall-rudataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(rudata_DATA)'; test -n "$(rudatadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(rudatadir)'; $(am__uninstall_files_from_dir)
tags: TAGS
TAGS:

ctags: CTAGS
CTAGS:


distdir: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	list='$(DISTFILES)'; \
	  dist_files=`for file in $$list; do echo $$file; done | \
	  sed -e "s|^$$srcdirstrip/||;t" \
	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
	case $$dist_files in \
	  */*) $(MKDIR_P) `echo "$$dist_files" | \
			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
			   sort -u` ;; \
	esac; \
	for file in $$dist_files; do \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  if test -d $$d/$$file; then \
	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
	    if test -d "$(distdir)/$$file"; then \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -fpR $(srcdir)/$$file "$(distdir)$$dir" || exit 1; \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    cp -fpR $$d/$$file "$(distdir)$$dir" || exit 1; \
	  else \
	    test -f "$(distdir)/$$file" \
	    || cp -p $$d/$$file "$(distdir)/$$file" \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
all-am: Makefile $(DATA)
installdirs:
	for dir in "$(DESTDIR)$(aschunkdir)" "$(DESTDIR)$(asdatadir)" "$(DESTDIR)$(asdepdatadir)" "$(DESTDIR)$(asnecdatadir)" "$(DESTDIR)$(cachunkdir)" "$(DESTDIR)$(cadatadir)" "$(DESTDIR)$(cadepdatadir)" "$(DESTDIR)$(cadnercdatadir)" "$(DESTDIR)$(canecdatadir)" "$(DESTDIR)$(canerdatadir)" "$(DESTDIR)$(commonalternativesdatadir)" "$(DESTDIR)$(commondatadir)" "$(DESTDIR)$(commonlangidentdatadir)" "$(DESTDIR)$(commonnecdatadir)" "$(DESTDIR)$(configdatadir)" "$(DESTDIR)$(csdatadir)" "$(DESTDIR)$(cydatadir)" "$(DESTDIR)$(cynecdatadir)" "$(DESTDIR)$(enchunkdir)" "$(DESTDIR)$(endatadir)" "$(DESTDIR)$(endepdatadir)" "$(DESTDIR)$(endnercdatadir)" "$(DESTDIR)$(ennecdatadir)" "$(DESTDIR)$(ennerdatadir)" "$(DESTDIR)$(eschunkdir)" "$(DESTDIR)$(escorefdatadir)" "$(DESTDIR)$(esdatadir)" "$(DESTDIR)$(esdepdatadir)" "$(DESTDIR)$(esdnercdatadir)" "$(DESTDIR)$(esnecdatadir)" "$(DESTDIR)$(esnerdatadir)" "$(DESTDIR)$(frdatadir)" "$(DESTDIR)$(glchunkdir)" "$(DESTDIR)$(gldatadir)" "$(DESTDIR)$(gldepdatadir)" "$(DESTDIR)$(gldnercdatadir)" "$(DESTDIR)$(glnecdatadir)" "$(DESTDIR)$(glnerdatadir)" "$(DESTDIR)$(itdatadir)" "$(DESTDIR)$(itnecdatadir)" "$(DESTDIR)$(oldesdatadir)" "$(DESTDIR)$(pkgdatadir)" "$(DESTDIR)$(ptchunkdir)" "$(DESTDIR)$(ptdatadir)" "$(DESTDIR)$(ptdnercdatadir)" "$(DESTDIR)$(ptnecdatadir)" "$(DESTDIR)$(ptnerdatadir)" "$(DESTDIR)$(rudatadir)"; do \
	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
	done
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	if test -z '$(STRIP)'; then \
	  $(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	    install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	      install; \
	else \
	  $(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	    install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	    "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'" install; \
	fi
mostlyclean-generic:

clean-generic:

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)
	-test . = "$(srcdir)" || test -z "$(CONFIG_CLEAN_VPATH_FILES)" || rm -f $(CONFIG_CLEAN_VPATH_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean: clean-am

clean-am: clean-generic clean-libtool mostlyclean-am

distclean: distclean-am
	-rm -f Makefile
distclean-am: clean-am distclean-generic

dvi: dvi-am

dvi-am:

html: html-am

html-am:

info: info-am

info-am:

install-data-am: install-aschunkDATA install-asdataDATA \
	install-asdepdataDATA install-asnecdataDATA \
	install-cachunkDATA install-cadataDATA install-cadepdataDATA \
	install-cadnercdataDATA install-canecdataDATA \
	install-canerdataDATA install-commonalternativesdataDATA \
	install-commondataDATA install-commonlangidentdataDATA \
	install-commonnecdataDATA install-configdataDATA \
	install-csdataDATA install-cydataDATA install-cynecdataDATA \
	install-enchunkDATA install-endataDATA install-endepdataDATA \
	install-endnercdataDATA install-ennecdataDATA \
	install-ennerdataDATA install-eschunkDATA \
	install-escorefdataDATA install-esdataDATA \
	install-esdepdataDATA install-esdnercdataDATA \
	install-esnecdataDATA install-esnerdataDATA install-frdataDATA \
	install-glchunkDATA install-gldataDATA install-gldepdataDATA \
	install-gldnercdataDATA install-glnecdataDATA \
	install-glnerdataDATA install-itdataDATA install-itnecdataDATA \
	install-oldesdataDATA install-pkgdataDATA install-ptchunkDATA \
	install-ptdataDATA install-ptdnercdataDATA \
	install-ptnecdataDATA install-ptnerdataDATA install-rudataDATA
	@$(NORMAL_INSTALL)
	$(MAKE) $(AM_MAKEFLAGS) install-data-hook
install-dvi: install-dvi-am

install-dvi-am:

install-exec-am:

install-html: install-html-am

install-html-am:

install-info: install-info-am

install-info-am:

install-man:

install-pdf: install-pdf-am

install-pdf-am:

install-ps: install-ps-am

install-ps-am:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic mostlyclean-libtool

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-aschunkDATA uninstall-asdataDATA \
	uninstall-asdepdataDATA uninstall-asnecdataDATA \
	uninstall-cachunkDATA uninstall-cadataDATA \
	uninstall-cadepdataDATA uninstall-cadnercdataDATA \
	uninstall-canecdataDATA uninstall-canerdataDATA \
	uninstall-commonalternativesdataDATA uninstall-commondataDATA \
	uninstall-commonlangidentdataDATA uninstall-commonnecdataDATA \
	uninstall-configdataDATA uninstall-csdataDATA \
	uninstall-cydataDATA uninstall-cynecdataDATA \
	uninstall-enchunkDATA uninstall-endataDATA \
	uninstall-endepdataDATA uninstall-endnercdataDATA \
	uninstall-ennecdataDATA uninstall-ennerdataDATA \
	uninstall-eschunkDATA uninstall-escorefdataDATA \
	uninstall-esdataDATA uninstall-esdepdataDATA \
	uninstall-esdnercdataDATA uninstall-esnecdataDATA \
	uninstall-esnerdataDATA uninstall-frdataDATA \
	uninstall-glchunkDATA uninstall-gldataDATA \
	uninstall-gldepdataDATA uninstall-gldnercdataDATA \
	uninstall-glnecdataDATA uninstall-glnerdataDATA \
	uninstall-itdataDATA uninstall-itnecdataDATA \
	uninstall-oldesdataDATA uninstall-pkgdataDATA \
	uninstall-ptchunkDATA uninstall-ptdataDATA \
	uninstall-ptdnercdataDATA uninstall-ptnecdataDATA \
	uninstall-ptnerdataDATA uninstall-rudataDATA
	@$(NORMAL_INSTALL)
	$(MAKE) $(AM_MAKEFLAGS) uninstall-hook
.MAKE: install-am install-data-am install-strip uninstall-am

.PHONY: all all-am check check-am clean clean-generic clean-libtool \
	distclean distclean-generic distclean-libtool distdir dvi \
	dvi-am html html-am info info-am install install-am \
	install-aschunkDATA install-asdataDATA install-asdepdataDATA \
	install-asnecdataDATA install-cachunkDATA install-cadataDATA \
	install-cadepdataDATA install-cadnercdataDATA \
	install-canecdataDATA install-canerdataDATA \
	install-commonalternativesdataDATA install-commondataDATA \
	install-commonlangidentdataDATA install-commonnecdataDATA \
	install-configdataDATA install-csdataDATA install-cydataDATA \
	install-cynecdataDATA install-data install-data-am \
	install-data-hook install-dvi install-dvi-am \
	install-enchunkDATA install-endataDATA install-endepdataDATA \
	install-endnercdataDATA install-ennecdataDATA \
	install-ennerdataDATA install-eschunkDATA \
	install-escorefdataDATA install-esdataDATA \
	install-esdepdataDATA install-esdnercdataDATA \
	install-esnecdataDATA install-esnerdataDATA install-exec \
	install-exec-am install-frdataDATA install-glchunkDATA \
	install-gldataDATA install-gldepdataDATA \
	install-gldnercdataDATA install-glnecdataDATA \
	install-glnerdataDATA install-html install-html-am \
	install-info install-info-am install-itdataDATA \
	install-itnecdataDATA install-man install-oldesdataDATA \
	install-pdf install-pdf-am install-pkgdataDATA install-ps \
	install-ps-am install-ptchunkDATA install-ptdataDATA \
	install-ptdnercdataDATA install-ptnecdataDATA \
	install-ptnerdataDATA install-rudataDATA install-strip \
	installcheck installcheck-am installdirs maintainer-clean \
	maintainer-clean-generic mostlyclean mostlyclean-generic \
	mostlyclean-libtool pdf pdf-am ps ps-am uninstall uninstall-am \
	uninstall-aschunkDATA uninstall-asdataDATA \
	uninstall-asdepdataDATA uninstall-asnecdataDATA \
	uninstall-cachunkDATA uninstall-cadataDATA \
	uninstall-cadepdataDATA uninstall-cadnercdataDATA \
	uninstall-canecdataDATA uninstall-canerdataDATA \
	uninstall-commonalternativesdataDATA uninstall-commondataDATA \
	uninstall-commonlangidentdataDATA uninstall-commonnecdataDATA \
	uninstall-configdataDATA uninstall-csdataDATA \
	uninstall-cydataDATA uninstall-cynecdataDATA \
	uninstall-enchunkDATA uninstall-endataDATA \
	uninstall-endepdataDATA uninstall-endnercdataDATA \
	uninstall-ennecdataDATA uninstall-ennerdataDATA \
	uninstall-eschunkDATA uninstall-escorefdataDATA \
	uninstall-esdataDATA uninstall-esdepdataDATA \
	uninstall-esdnercdataDATA uninstall-esnecdataDATA \
	uninstall-esnerdataDATA uninstall-frdataDATA \
	uninstall-glchunkDATA uninstall-gldataDATA \
	uninstall-gldepdataDATA uninstall-gldnercdataDATA \
	uninstall-glnecdataDATA uninstall-glnerdataDATA uninstall-hook \
	uninstall-itdataDATA uninstall-itnecdataDATA \
	uninstall-oldesdataDATA uninstall-pkgdataDATA \
	uninstall-ptchunkDATA uninstall-ptdataDATA \
	uninstall-ptdnercdataDATA uninstall-ptnecdataDATA \
	uninstall-ptnerdataDATA uninstall-rudataDATA


install-data-hook:
	-@ldconfig &> /dev/null || echo "Could not run 'ldconfig'. If you get installation errors below, execute 'ldconfig' as root and try 'make install' again.";
	@echo "Indexing language dictionaries. This will take some minutes.";
	@for lg in $(languages); do \
	   echo "Installing files for language '"$$lg"'. Please wait."; \
	   echo "  - Creating "$$lg" dictionary..."; \
	   $(top_builddir)/src/utilities/dicc-management/bin/build-dict.sh $(top_builddir)/data/$$lg/dictionary/header $(top_builddir)/data/$$lg/dictionary/entries/* > $(DESTDIR)/$(pkgdatadir)/$$lg/dicc.src; \
	   for sublg in $(variants); do \
	      if (echo $$sublg | grep -q $$lg/); then \
		 echo "  - Creating "$$sublg" variant dictionary..."; \
		 $(top_builddir)/src/utilities/dicc-management/bin/build-dict.sh $(top_builddir)/data/$$sublg/dictionary/header $(top_builddir)/data/$$lg/dictionary/entries/* $(top_builddir)/data/$$sublg/dictionary/entries/* > $(DESTDIR)/$(pkgdatadir)/$$sublg/dicc.src; \
	      fi;\
	   done; \
	   if (test -f "$(DESTDIR)/$(pkgdatadir)/$$lg/alternatives-phon.dat" ); then \
	      echo "  - Creating "$$lg" phonetic dictionary..."; \
              phRul=`cat $(DESTDIR)/$(pkgdatadir)/$$lg/alternatives-phon.dat | grep PhoneticRules | cut -d' ' -f2`; \
              phDic=`cat $(DESTDIR)/$(pkgdatadir)/$$lg/alternatives-phon.dat | grep PhoneticDictionary | cut -d' ' -f2`; \
	      cat $(DESTDIR)/$(pkgdatadir)/$$lg/dicc.src | LD_LIBRARY_PATH=$(LD_LIBRARY_PATH):$(DESTDIR)/$(libdir) $(DESTDIR)/$(bindir)/dicc2phon $(DESTDIR)/$(pkgdatadir)/$$lg/$$phRul > $(DESTDIR)/$(pkgdatadir)/$$lg/$$phDic; \
           fi; \
	done

uninstall-hook:
	@rm -rf $(DESTDIR)/$(pkgdatadir)

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
